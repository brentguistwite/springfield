# Contract

When you complete a user story, emit exactly: <story-pass>US-XXX</story-pass>
When ALL stories in this plan have been completed, emit exactly: <promise>COMPLETE</promise>
Do not write to .springfield/ from inside the worktree — Springfield owns that directory.
Do not invoke springfield CLI subcommands; you are already inside a managed run.